/* SheetJS Community Edition, pinned to an explicit official release. */
self.importScripts('https://cdn.sheetjs.com/xlsx-0.20.3/package/dist/xlsx.full.min.js');
self.onmessage=({data})=>{try{const workbook=self.XLSX.read(data,{type:'array',cellDates:true,dateNF:'yyyy-mm-dd',sheetRows:20002}),sheet=workbook.Sheets[workbook.SheetNames[0]];if(!sheet)throw new Error('No worksheet found');const range=self.XLSX.utils.decode_range(sheet['!fullref']||sheet['!ref']||'A1');if(range.e.r>20000||range.e.c>149)throw new Error('Maximum 20,000 rows and 150 columns');const matrix=self.XLSX.utils.sheet_to_json(sheet,{header:1,defval:'',raw:false,dateNF:'yyyy-mm-dd',blankrows:false}),headers=(matrix.shift()||[]).map((h,i)=>String(h).trim()||`Column ${i+1}`);self.postMessage({sheetName:workbook.SheetNames[0],headers,rows:matrix.map(row=>row.map(v=>String(v??'')))});}catch(e){self.postMessage({error:e.message||'Unable to read this workbook'})}};
